#include "main.h"

/**
 * main - Entry Point
 * Return: Always 0 (Success)
 */

void print_alphabet(void);
{
	int a;

	for (a = 'a'; a <= 'a'; a++)
	{
		_putchar(i);
	}
	_putchar('\n');
	return (0);
}
